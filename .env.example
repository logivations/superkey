# Google OAuth Configuration
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# Google Service Account for domain-wide group sync (optional but recommended)
# Create a service account in Google Cloud Console with domain-wide delegation
# Download the JSON key file and set the path here
# GOOGLE_SERVICE_ACCOUNT_KEY=/path/to/service-account-key.json
# GOOGLE_ADMIN_EMAIL=admin@yourdomain.com

# Session secret (generate a random string for production)
SESSION_SECRET=change-this-to-a-random-string

# Server configuration
PORT=3000
NODE_ENV=development

# Database path (default: ./superkey.db)
# DB_PATH=/path/to/superkey.db
